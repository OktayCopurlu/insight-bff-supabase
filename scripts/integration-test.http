### Basic integration tests (use with REST Client extension or curl manually)

# 1. List articles
GET {{BASE_URL}}/functions/v1/news-processor/articles

# 2. Get single article (replace ARTICLE_ID after first list)
GET {{BASE_URL}}/functions/v1/news-processor/articles/ARTICLE_ID

# 3. Generate explanation
POST {{BASE_URL}}/functions/v1/news-processor/articles/ARTICLE_ID/explanation

# 4. Chat (simple) — now via BFF server, not Edge Function (example path)
# POST http://localhost:4000/cluster/CLUSTER_ID/chat?lang=en
# Content-Type: application/json
#
# {
#   "message": "Summarize this in one sentence",
#   "chatHistory": []
# }

# 5. Coverage analyze (ephemeral)
POST {{BASE_URL}}/functions/v1/coverage-analyzer/analyze
Content-Type: application/json

{
  "articleId": "ARTICLE_ID"
}

# 6. Quiz placeholder
GET {{BASE_URL}}/functions/v1/quiz-generator/quiz/ARTICLE_ID
